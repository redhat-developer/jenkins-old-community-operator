apiVersion: jenkins.io/v1alpha3
kind: Casc
metadata:
  name: example-casc
spec:
  jenkinsName: 'example'
  configurationAsCode:
    secret: 
      name: user-secret
    configurations: 
    - name: jenkins-example-user-configuration
  groovyScripts:
    configurations:
    - name: jenkins-example-user-configuration
